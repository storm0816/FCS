# 对象存储（火山引擎 TOS）凭据配置说明
# ----------------------------------------------------------------------------
# 本项目使用 @volcengine/tos-sdk 访问对象存储，凭据直接配置在 config.json
# 的 objectStorage 节点中（accessKey / secretKey 字段），无需环境变量。
#
# 配置示例（config.json）：
#   "objectStorage": {
#     "enabled": true,
#     "region": "cn-shanghai",
#     "endpoint": "tos-cn-shanghai.volces.com",
#     "bucket": "mobilefile",
#     "prefix": "file/b2sumkline",
#     "accessKey": "AKTP...",
#     "secretKey": "...",
#     "localDir": "b2sumdata"
#   }
#
# 文件命名规则（{ip}-{market}-NIG.b2sum）、b2sum 格式、.NIG 后缀剥离为
# 系统固定常量（src/objectStorage.js），不在配置中出现。
#
# 切勿把真实密钥提交进仓库。
# ----------------------------------------------------------------------------
