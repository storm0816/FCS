# FCS（File Check System）· 产品设计文档（PRD）

> 文档版本：v2.0（历史 PRD 草稿）｜仓库发布版本：v0.1.0 ｜ 适用对象：行情监控 / 数据质量团队
> 变更：v2.0 在原技术设计基础上补充产品管理要素——问题量化、成功指标体系、用户角色与干系人、用户故事、验收标准、路线图、风险与合规，重构为完整 PRD。
>
> 阅读指引：第 1–6 章为「为什么做 / 给谁做 / 做成什么样」（产品决策）；第 7–11 章为「怎么做」（算法、数据、接口、前端、安全）；第 12–14 章为「节奏与边界」（路线图、风险、假设）。

---

## 1. 背景与问题陈述（Why）

### 1.1 现状
有多台行情服务器，每台承载 **64+ 个市场**，各自生成并存储历史日 K 线数据。由于数据源、合并脚本、存储链路的差异，不同服务器上「同一市场 + 同一代码」的数据可能出现不一致（缺数据、截断、字段错位、回测修复未同步等）。

### 1.2 痛点（量化）
| 痛点 | 影响 | 现状代价 |
|------|------|----------|
| 跨服务器一致性无人核对 | 错误数据流入下游策略/回测，造成"幽灵亏损" | 依赖人工抽检，覆盖 < 5% |
| 数据异常发现滞后 | 异常往往在下一次人工复盘或客户投诉时才暴露 | 平均发现周期以「天/周」计 |
| 缺乏分区隔离 | 不同机房/可用区数据本就允许不同，混比产生大量误报 | 噪音淹没真实问题 |
| 配置靠手改 JSON | 运维接入新服务器易出错、无校验 | 单次接入 > 15 分钟且易错 |

### 1.3 现有方案为什么不够
- 人工逐台 `diff` 不可行：64 市场 × 海量代码 × N 台服务器，组合爆炸。
- 通用监控（CPU/磁盘）不感知"数据内容是否一致"。
- 多数一致性工具是"主从比对"，无法处理"多副本对等、谁是少数派未知"的场景。

### 1.4 本平台定位
**一个离线、分区隔离、少数派判定的 K 线数据一致性巡检平台**——只负责**发现异常并精准定位**，修复交由下游/人工。

---

## 2. 产品目标与成功指标

### 2.1 业务目标
1. **自动巡检**：SSH 远程拉取各服务器哈希，免去人工逐台核对。
2. **精准判定**：同分区内少数派不一致即判异常，缺失/冲突/不可达分类清晰。
3. **分区隔离**：服务器按 Zone 组织，比对仅同分区内进行。
4. **多并发**：两级并发池支撑 64+ 市场大批量巡检。
5. **运营闭环**：Web 后台完成「配置 → 巡检 → 报告」全链路，配置落本地 `config.json`。

### 2.2 用户目标
- 数据质量工程师：一眼掌握整体数据质量态势，快速定位异常到「服务器 + 代码」。
- 运维工程师：可视化接入服务器、按分区隔离、并发巡检不压垮机器。
- 技术负责人：可控的压力、可审计的配置、清晰的派单依据。

### 2.3 成功指标（可衡量）

> 原则：可观测、可归因、有基线与目标值。以下为产品自身健康度指标（区别于被检数据的 healthRate）。

| 类型 | 指标 | 口径 | 基线 | 目标 |
|------|------|------|------|------|
| **北极星** | 数据一致性巡检覆盖率 | 本周期完成有效巡检的 (分区·市场·代码) 组合数 ÷ 应巡检总数 × 100%；有效=该组合至少 2 台成功采集并得出判定 | Mock 演示 100% | ≥ 99% |
| 驱动 | 异常定位准确率 | 带 `expected/actual` 且经复核确认的 anomaly ÷ 全部 anomaly 标记 | — | ≥ 98% |
| 驱动 | 全量巡检耗时 P95 | 一次「全分区+全市场」巡检端到端耗时（演示 756 代码） | — | < 5s |
| 驱动 | 配置就绪率 | 已正确配置 `zone`+`hashCmd`+凭据的服务器 ÷ 总服务器数 | — | 100% |
| **健康（护栏）** | SSH 连接成功率 | 成功采集 ÷ 发起采集（剔除计划维护） | — | ≥ 99% |
| 健康 | 误报率 | 经复核非真实异常的 hash_mismatch 标记 ÷ 全部标记（平局 conflict 不计入） | — | ≤ 2% |
| 健康 | 服务可用性 | Web 可读写配置 + API 可达时长占比 | — | ≥ 99.9% |
| 健康 | 凭据零泄露 | 因本平台导致的凭据外泄事件数 | 0 | 0 |

> 说明：被检数据的 `healthRate`（一致代码数 ÷ 总代码数）是**巡检输出**，非产品健康度指标，二者不可混淆。

---

## 3. 用户角色与利益相关者

### 3.1 角色画像（Persona）
| 角色 | 关注点 | 典型操作 |
|------|--------|----------|
| 数据质量工程师（主用户） | 数据是否一致、异常在哪 | 发起巡检、看报告、派单修复 |
| 运维工程师 | 接入是否简单、机器压力 | 增删服务器、设分区、调并发 |
| 技术负责人 | 风险可控、配置可审计 | 审配置、定并发上限、接告警 |

### 3.2 利益相关者地图（RACI）
| 干系人 | 配置管理 | 巡检执行 | 异常处置 | 安全合规 |
|--------|----------|----------|----------|----------|
| 数据质量工程师 | C | R/A | R | I |
| 运维工程师 | R | C | C | C |
| 技术负责人 | A | C | A | A |
| 安全负责人 | I | I | I | R/A |

> R=负责执行，A=最终 accountable，C=被咨询，I=知会。

---

## 4. 用户故事（User Stories）

> 标准格式：**作为 [角色]，我希望 [行为]，以便 [价值]**。

### Epic 1 · 配置管理
- 作为运维工程师，我希望在后台可视化地增删改服务器（IP / 端口 / 用户 / 密码）并选择所属分区，以便无需手改 JSON 即可完成接入。
- 作为数据质量工程师，我希望管理市场列表（增删、启用开关），以便控制哪些市场纳入全量巡检。
- 作为技术负责人，我希望配置分区（Zone）并设置每分区并发上限，以便按机房/可用区隔离比对并控制压力。

### Epic 2 · 巡检执行
- 作为数据质量工程师，我希望选择「指定分区 + 指定市场」或「全量」发起巡检，以便按需聚焦问题范围、节省时间。
- 作为运维工程师，我希望巡检以多并发执行且有进度反馈，以便在 64+ 市场大批量下快速拿到结果且不压垮机器。

### Epic 3 · 异常发现与报告
- 作为数据质量工程师，我希望看到按「分区 → 市场」分组的异常明细（含 `expected/actual` 哈希），以便快速定位是哪台服务器的哪个代码出了问题。
- 作为技术负责人，我希望 `missing` / `conflict` / `unreachable` 与 `hash_mismatch` 被清晰区分，以便派单时不会把连接故障误当数据异常。
- 作为数据质量工程师，我希望看到全局健康率与异常数汇总卡片，以便一眼掌握整体数据质量态势。

### Epic 4 · 安全与合规
- 作为安全负责人，我希望密码可迁移到私钥认证、且配置文件权限受控，以便降低凭据泄露风险。

---

## 5. 功能需求与验收标准

优先级：**P0**=必须（MVP），**P1**=重要（紧随），**P2**=增强。

### 5.1 分区管理（Zone） · P0
- 功能：增删改分区（id / 名称 / 并发上限）；服务器绑定分区。
- 验收标准：
  - **Given** 用户新增分区 z3 并设置并发=3，**When** 保存，**Then** `config.json` 的 `zones[]` 出现该分区且持久化。
  - **Given** 某服务器绑定 z1，**When** 巡检 z1，**Then** 该服务器仅与 z1 内其他服务器比对，绝不与 z2 比对。

### 5.2 服务器配置 · P0
- 功能：列表展示分区/名称/IP/端口/用户/认证；新增/编辑/删除；认证支持 `password` 与 `key`。
- 验收标准：
  - **Given** 填写 host/port/username/password 并选 zone，**When** 保存，**Then** 配置落盘且下次巡检可用。
  - **Given** 选择私钥认证，**When** 保存，**Then** 使用 `privateKey` 字段而非 password 连接。

### 5.3 市场配置 · P0
- 功能：增删市场（code/name），`enabled` 开关；全量巡检仅扫 `enabled=true`。
- 验收标准：
  - **Given** 某市场 `enabled=false`，**When** 发起「全量巡检」，**Then** 该市场被跳过。

### 5.4 多并发巡检执行 · P0
- 功能：选分区范围（全部分区 / 指定）+ 市场范围（全部 / 指定）+ **对比方式**（单区对比 / 多区合并对比）；一键启动；实时进度。
- 验收标准：
  - **Given** 全局 concurrency=4、z1 concurrency=2，**When** 全量巡检，**Then** 同时运行的区 ≤4、区内同时运行的市场 ≤2。
  - **Given** 指定 `zones:["z2"], markets:["SH","SZ"]`，**When** 执行，**Then** 仅返回 z2 的 SH/SZ 结果。
  - **Given** 选「多区合并对比」+ `zones:["z1","z2"]`，**When** 执行，**Then** 将 z1、z2 内所有服务器合并为一个对比池，跨区一起判定（结果以「多区合并对比」单组返回，并标注每台异常服务器所属分区）。

### 5.5 异常报告与可视化 · P0
- 功能：汇总卡片（健康率/异常/冲突/不可达）+ 按「分区→市场」分组明细；可展开看 `expected/actual`；四类异常配色区分。
- 验收标准：
  - **Given** 某 (分区,市场,代码) 在 N 台中 1 台哈希不同，**When** 巡检完成，**Then** 该条以 `hash_mismatch` 出现，含 `expected`(众数)、`actual`(本机)。
  - **Given** 2 vs 2 平局，**When** 巡检完成，**Then** 标记 `conflict` 而非任意判异常。
  - **Given** 某服务器 SSH 失败，**When** 巡检完成，**Then** 该服务器该市场标 `unreachable`，不计入数据异常。

### 5.6 配置持久化与本地化 · P0
- 功能：`GET/PUT /api/config` 读写 `config.json`；无需数据库。
- 验收标准：**Given** 前端保存，**When** 服务重启，**Then** 配置仍在。

---

## 6. 非目标（Non-goals · 明确边界，防范围蔓延）

- ❌ **不修复数据**：只发现异常并告警，修复由下游/人工处理。
- ❌ **不做实时行情校验**：仅对「已生成的历史日 K」做离线巡检。
- ❌ **不内置调度器**：周期巡检由系统 cron / 第三方调用 `/api/inspect` 实现。
- ❌ **默认不跨分区比对**：分区是默认比对边界，跨分区差异属正常——但平台**支持显式开启「多区合并对比」**，将若干选中分区的服务器合并为一个池跨区判定（适用于「各区应为同一份数据副本、需全局一致性」的场景）。
- ❌ **不做权限多租户**：单团队内网使用，不实现账号体系（见风险章）。
- ❌ **不自动回写修复**：不主动改任何服务器上的数据文件。

---

## 7. 核心算法

### 7.1 哈希采集
每台服务器配置一个 **`hashCmd`** 模板（可按服务器不同），或用全局 **`settings.hashCmdTemplate`** 统一模板；平台用 `{market}` / `{marketName}` / `{baseDir}` 占位符替换后远程执行（真实模式）。脚本须按行返回：
```
<代码>\t<哈希>
<代码>\t<哈希>
...
```
> 哈希由**服务器侧**计算，仅传输「代码+哈希」两列，网络开销极小，适合 64+ 市场 × 海量代码。
> 真实环境常需 `sudo` 提权：当 `settings.useSudo=true` 时，平台自动将命令包装为 `echo '<password>' | sudo -S bash -lc '<escaped cmd>'`（复刻原始 Python `SshFetcher._wrap`），以登录用户 `username` 切到 `settings.sudoUser` 执行。原始生产配置即采用此方式（端口 2345、`dzhhq` 登录、`baseDir=/home/MobileServer/DATA`、对 `{baseDir}/{market}/history/day` 下 `*.NIG` 逐文件 `sha256sum`）。

### 7.2 少数派判定（核心，两种对比方式）

平台提供两种**对比方式（compareMode）**，通过巡检请求参数 `compareMode` 选择，UI 在执行面板以单选切换：

| 对比方式 | 取值 | 语义 | 适用场景 |
|---|---|---|---|
| **单区对比** | `per-zone`（默认） | 每个选中的分区**各自独立**比对，结果按分区分组 | 分区数据本就允许不同，需保留分区隔离 |
| **多区合并对比** | `merged` | 将选中分区内**所有服务器合并为一个对比池**，跨区一起判定 | 各区应是同一份数据副本、需全局一致性，或分区机器数不足 3 台时借其他区凑足多数 |

对某一**对比组** `G`（单区模式下一个分区、合并模式下多个分区的并集）、市场 `M`、代码 `C`，从组内 N 台服务器采集哈希集合 `H = {S1:h1, ..., SN:hN}`：
1. **全部一致** → 健康。
2. **存在差异**：
   - 统计各哈希出现次数，取最多者为**众数（基准值）**。
   - 次数 **少于众数** 的服务器判为**异常方（少数派）**，记录 `expected=基准`、`actual=本机`，并附带该服务器 `serverName` 与所属 `zoneName`。
   - **平局（冲突）**：两及以上哈希并列最多（如 2 vs 2），无法定少数 → 标记 `conflict`，全部入复核。
3. **缺失**：某服务器对该 (M,C) 无输出 → `missing`。
4. **不可达**：SSH 失败 → `unreachable`（与数据异常区分）。

> 关键约束：少数派判定依赖「多数即正确」，**判定组内需 ≥3 台服务器**才能确立众数。
> - **单区模式**下，某分区仅 2 台且不一致时保守标 `conflict`（无法定少数）。
> - **合并模式**下，把多个分区凑成一个 ≥3 台的池，即可让多数（健康）压过少数（异常）——例如 5 台机中 1 台异常，合并后该异常机从「单区 1v1 平局」变为「跨区少数派」，被正确识别为 `hash_mismatch`。

### 7.3 判定流程（伪代码）
```
compareMode = 请求参数.per-zone | merged
if compareMode == merged:
  pool = servers where s.zone in 选中分区          # 合并选中分区所有服务器
  groups = [ { name:"多区合并对比", servers: pool } ]
else:
  groups = [ { name:Z.name, servers: servers where s.zone==Z.id } for Z in 选中分区 ]

for g in groups:
  for market M in 选中市场:
    perServer = { S.id: {code:hash} for S in g.servers }   # 组内并发 SSH
    for code C in union(g 内各服务器返回的 code):
      present = [e for e in g.servers if e.hash != null]
      if present 全部相同: 一致
      else:
        groups_map = group_by(hash, present)
        if 存在并列最多: 标记 conflict, 全部 present 入复核
        else: 标记 e.hash != modeHash 的服务器为 异常(minority)，附带 zoneName
      for S in g.servers where hash==null 且无错误: 标记 missing(C)
      for S in g.servers where 采集报错: 标记 unreachable(S)
```

### 7.4 两级并发池（多并发）
Node 单线程事件循环 + 异步并发等价于「线程池」。本平台两级受控并发：
- **第一级（分区级）**：各分区并行，受全局 `settings.concurrency` 约束。
- **第二级（市场级）**：区内各市场并行，受本分区 `zones[].concurrency` 约束；区内各服务器 SSH 拉取进一步 `Promise.all` 并行。

两处上限均可在「运行设置 / 分区配置」调整，避免压垮服务器。

### 7.5 结果聚合
- 按 (分区, 市场) 汇总：总代码数、一致数、异常数、缺失数、冲突数、不可达服务器数。
- 全局：异常清单、冲突清单、健康率。
- 每条异常：`{ zone, market, code, serverId, type, expected, actual, detail }`。

---

## 8. 数据模型与配置

全部存放本地 `config.json`，由平台读写。

```json
{
  "zones": [
    { "id": "prod", "name": "生产环境(percode)", "concurrency": 5 }
  ],
  "servers": [
    {
      "id": "srv-206",
      "zone": "prod",
      "name": "行情机-B 10.36.0.206(基线)",
      "host": "10.36.0.206",
      "port": 2345,
      "username": "dzhhq",
      "auth": { "type": "password", "password": "******" },
      "hashCmd": "",
      "baseline": true,
      "mockFaulty": false
    }
  ],
  "markets": [
    { "code": "SH", "name": "上海日线", "enabled": true },
    { "code": "SZ", "name": "深圳日线", "enabled": true },
    { "code": "BJ", "name": "北京日线", "enabled": true }
  ],
  "settings": {
    "concurrency": 5,
    "sshTimeout": 60000,
    "execTimeout": 60000,
    "mockMode": true,
    "baseDir": "/home/MobileServer/DATA",
    "useSudo": true,
    "sudoUser": "root",
    "hashCmdTemplate": "cd {baseDir}/{market}/history/day 2>/dev/null && for f in $(find . -maxdepth 1 -name \"*.NIG\" 2>/dev/null | xargs -n1 basename | sort -u | head -n 200); do [ -f \"$f\" ] && sha256sum \"$f\" | awk '{print $2\"\\t\"$1}'; done; true"
  }
}
```

| 字段 | 说明 |
|------|------|
| `zones[].id` | 分区唯一标识 |
| `zones[].name` | 分区名称（如生产环境(percode)） |
| `zones[].concurrency` | 本分区市场级并发上限 |
| `servers[].id` | 服务器唯一标识，用于结果关联 |
| `servers[].zone` | 所属分区 id（决定与哪些服务器比对） |
| `servers[].host/port/username` | SSH 连接信息 |
| `servers[].auth.type` | `password`（密码）或 `key`（私钥，`privateKey` 字段） |
| `servers[].hashCmd` | 远程哈希命令模板（可空，回退 `settings.hashCmdTemplate`）；支持 `{market}`/`{marketName}`/`{baseDir}` |
| `servers[].baseline` | 是否为基线机（元数据，UI 标注，不参与判定） |
| `servers[].mockFaulty` | Mock 模式是否注入异常（仅演示） |
| `markets[].code` | 市场代码，传入 `hashCmd` 的 `{market}` |
| `markets[].enabled` | 是否纳入全量巡检 |
| `settings.concurrency` | 分区级全局并发上限 |
| `settings.mockMode` | 开启后跳过 SSH，用本地确定性数据演示算法 |
| `settings.baseDir` | 远端数据根目录，注入 `hashCmdTemplate` 的 `{baseDir}` |
| `settings.useSudo` / `sudoUser` | 是否以 sudo 提权及目标用户（默认 root） |
| `settings.hashCmdTemplate` | 全局哈希命令模板（逐文件 `sha256sum`，输出 `代码\t哈希`）；**默认哈希该市场全部代码，不做数量截断** |
| `settings.maxCodes` | 可选性能上限：`>0` 时截断最终输出行数（如 `200`），`0` = 不限制（默认）。旧版写死的 `head -n 200` 已移除，避免静默丢数据与跨机比对集合错位 |
| `settings.hashAlgo` | 哈希算法（sha256/sha1/md5，仅记录用） |

---

## 9. API 设计

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET`  | `/api/config`  | 获取完整配置 |
| `PUT`  | `/api/config`  | 保存完整配置到 `config.json` |
| `POST` | `/api/inspect` | 执行巡检，body：`{"zones":"all","markets":"all","compareMode":"per-zone"` `| "merged"}`；`compareMode` 省略默认 `per-zone` |
| `GET`  | `/api/health`  | 服务健康检查 |

`POST /api/inspect` 返回结构：
```json
{
  "summary": { "zones": 2, "markets": 12, "totalCodes": 756, "consistentCodes": 702,
               "anomalies": 54, "conflicts": 0, "unreachableServers": 0, "healthRate": 0.9286 },
  "zones": [
    {
      "zone": "z1", "name": "华东区",
      "markets": [
        {
          "market": "SH", "totalCodes": 63, "consistentCodes": 54,
          "anomalies": [ { "code":"SH100007","serverId":"srv2","type":"hash_mismatch",
                           "expected":"7acb...","actual":"3038...","detail":"" } ],
          "missing": [], "conflicts": [], "unreachable": []
        }
      ]
    }
  ],
  "elapsedMs": 1234
}
```

---

## 10. 前端功能

单页应用（原生 HTML/JS/CSS，无构建步骤）：
1. **① 分区配置**：增删改分区（id/名称/并发上限）；服务器下拉绑分区。
2. **② 服务器配置**：列表展示分区/名称/IP/端口/用户/认证；新增/编辑/删除；密码本地保存（明文入 JSON，见安全章）。
3. **③ 市场配置**：列表展示 code/name/`enabled` 开关；增删；全量仅扫 `enabled=true`。
4. **④ 运行设置**：全局并发上限、Mock 模式开关；保存全部到 `config.json`。
5. **⑤ 执行巡检（多并发）**：分区范围（全部/指定）+ 市场范围（全部/指定）+ **对比方式**（单区对比 / 多区合并对比）单选；一键启动；实时进度。
6. **⑥ 巡检结果**：汇总卡片（健康率/异常/冲突/不可达）+ 按**分区→市场**分组异常明细（可展开 `expected/actual`）；四类异常配色区分。

---

## 11. 安全与合规

1. **凭据明文**：密码直接写入本地 `config.json`。建议：`chmod 600` 限制权限；生产改用 `auth.type=key` 私钥（密钥不落地配置文件）；或用环境变量/密钥管理服务注入（平台预留 `privateKey` 字段）。
2. **传输安全**：SSH 本身加密；`hashCmd` 仅做 `{market}` 占位符替换，**不拼接用户输入**，避免命令注入。
3. **接口暴露**：Web 默认监听本地；团队访问前置反向代理 + 访问控制，勿将 Web/22 端口直暴露于公网。
4. **合规**：配置含服务器 IP、市场代码等，属内部运营数据；日志不落明文密码；如涉及个人信息处理须遵从《个人信息保护法》最小必要原则（本场景不直接处理个人信息，但告警推送链路需注意脱敏）。
5. **Mock 模式**：仅演示，开启后不连任何真实服务器。

---

## 12. 路线图（Now / Next / Later）

| 阶段 | 范围 | 关键交付 |
|------|------|----------|
| **Now（已交付 v1.1）** | 核心闭环 | 分区管理、服务器/市场配置、两级并发、少数派判定、Web 报告、Mock 模式、本地 `config.json` |
| **Next（v1.2–v1.3）** | 运营增强 | ① 定时巡检（cron 调 `/api/inspect`）② 告警推送（企业微信/钉钉）③ 基准机模式（指定基准哈希）④ 结果落盘 + 历史趋势 ⑤ 私钥认证加固 |
| **Later（v2.0+）** | 平台化 | ① SFTP 取数兜底（无哈希脚本时）② 自动修复建议 ③ 多租户/权限账号 ④ 与数据质量工单系统联动 ⑤ 跨周期异常收敛看板 |

> 决策提示：Next 阶段优先级建议按"价值/成本"排序——告警推送（低成本高价值）先于 SFTP 兜底（高成本）。

---

## 13. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 少数派误判（分区 <3 台） | 2 vs 2 无法定少数，漏判 | 每分区 ≥3 台；平局保守标 `conflict` 交人工 |
| 凭据明文泄露 | 服务器失陷 | `chmod 600` + 私钥认证 + 反向代理鉴权 |
| SSH 连接风暴 | 压垮行情机 | 两级并发上限 + 连接/执行超时 |
| 命令注入 | 远程执行风险 | 仅占位符替换，禁止拼接用户输入 |
| 大规模性能 | 巡检超时 | 两级并发池 + 指定分区/市场缩小范围 |
| 单点 Web 无鉴权 | 内网越权配置 | 内网部署 + 反向代理鉴权（Next 阶段） |

---

## 14. 假设与约定

- 远程哈希脚本由使用方提供，输出严格为 `CODE<TAB>HASH` 行格式（附录 A）。
- 哈希对比**默认仅同一分区内**进行；跨分区差异属正常。**显式选择「多区合并对比」时**，将选中分区所有服务器合并为一个池跨区判定（适用于各区应为同份副本的场景）。
- 同一 (对比组, 市场) 的代码集合以「对比组内各服务器返回的并集」为准；单台缺失视为 `missing`。
- 市场代码直接作为 `{market}` 占位符传入，使用方脚本需能据此定位数据。
- 少数派判定依赖「多数即正确」；平局场景保守标 `conflict` 交人工复核。
- 演示配置每分区 3 台（其中 1 台 `mockFaulty`），以体现少数派效果；健康率约 0.8571。

---

### 附录 A：远程哈希脚本示例（Python）
```python
#!/usr/bin/env python3
# /opt/kline/hash_market.py --market SH
import sys, hashlib, glob, os
market = sys.argv[sys.argv.index("--market")+1]
base = f"/data/kline/{market}"
for path in sorted(glob.glob(f"{base}/*.csv")):
    code = os.path.splitext(os.path.basename(path))[0]
    h = hashlib.sha256(open(path,"rb").read()).hexdigest()
    print(f"{code}\t{h}")
```

### 附录 B：判定示例（同分区内）
| 场景 | S1 | S2 | S3 | S4 | 判定 |
|------|----|----|----|----|------|
| 一致 | A | A | A | A | 健康 |
| 单点异常 | A | A | **B** | A | S3 少数派异常 |
| 两点异常 | A | A | **B** | **B** | S3,S4 少数派异常 |
| 平局冲突 | A | A | **B** | **B**(另组) | 冲突，全部复核 |
| 缺失 | A | A | *缺* | A | S3 缺失异常 |
| 不可达 | A | A | ⚠SSH | A | S3 unreachable（非数据异常） |
